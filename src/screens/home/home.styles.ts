import { StyleSheet } from "react-native";
export default StyleSheet.create({
  container: {
    alignItems: "center",
    paddingTop: 60,
  },
  buttons: {
    flexDirection: "column",
  },
  logo: {
    height: 150,
    maxWidth: "60%",
    resizeMode: "contain",
  },
});
